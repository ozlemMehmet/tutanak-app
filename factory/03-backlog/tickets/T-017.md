# T-017: Web AppShell, rota haritasi ve oturum korumasi

- **Repo:** main
- **QA modu:** izole
- **Boyut:** M
- **Bagimli oldugu ticketlar:** T-003
- **Iliskili PRD maddeleri:** H-08 (altyapi), tum kimlikli hikayelerin on kosulu
- **Durum:** todo

## Kapsam
Ofis kullanicisinin (Selin/Kaan) uygulamayi bir butun olarak kullanabilmesi icin gereken kabuk:
`design.md` §1 Ekran Envanteri'ndeki TUM rotalarin router'da tanimlanmasi, korumali rotalar icin
oturum guard'i, erisim token'inin saklanmasi/okunmasi ve AppShell (uygulama adi, oturum acmis
kullanici bilgisi, cikis).

Bu ticket ekranlarin ICERIGINI teslim etmez; ekranlar T-018..T-022'de doldurulur. Burada teslim
edilen sey, o ekranlara ulasilabilmesini saglayan iskelettir.

Entegrasyon QA baglami (`factory/08-integration/e2e-report.md`): backend tum hikayeleri karsiliyor
ama web'de yalnizca 3 rota var (`/`, `/reports/:id`, `/t/:token`) ve hicbiri girise/olusturmaya
baglanmiyor; `apps/web/src/api/access-token.ts` icinde "giris ekrani (henuz yazilmadi)" yorumu
duruyor. DoD'un 12 maddesinden 9'u bu bosluk yuzunden FAIL.

## Kabul Kriterleri (test edilebilir)
- [ ] Router `design.md` §1'deki rotalarin tamamini tanimlar: `/login`, `/register`, `/reports`, `/reports/new`, `/reports/:id`, `/subscription`, `/t/:token`.
- [ ] Saklanmis token YOKKEN korumali bir rotaya (`/reports`, `/reports/new`, `/reports/:id`, `/subscription`) gidildiginde kullanici `/login`'e yonlendirilir ve hedef rota `redirectTo` olarak korunur.
- [ ] Saklanmis token VARKEN ayni rotalar render edilir ve AppShell, `GET /me` yanitindaki kullanici e-postasini gosterir.
- [ ] `/t/:token` ve `/login` + `/register` rotalari token OLMADAN erisilebilir kalir (kiraci akisi oturum gerektirmez — H-06).
- [ ] Herhangi bir API cagrisi 401 dondugunde saklanan oturum temizlenir ve kullanici `/login`'e yonlendirilir (design.md, AppShell seviyesinde global davranis).
- [ ] AppShell'deki "Cikis" eylemi token'i siler ve `/login`'e goturur; sonrasinda korumali rotaya donus yeniden `/login`'e yonlendirir.

## Kapsam DISI
- LoginPage/RegisterPage form icerigi ve hata durumlari — T-018.
- Sablon secimi ve tutanak olusturma formu — T-019.
- Tutanak detay ekraninin tamamlanmasi (PDF butonu vb.) — T-020.
- Liste/arama ekrani — T-021; abonelik ekrani — T-022.
- Yeni endpoint veya API degisikligi: YOK; bu ticket yalnizca mevcut sozlesmeyi tuketir.

## Teknik Notlar (varsa)
- `apps/web/src/api/access-token.ts` bugun yer tutucudur; gercek saklama/okuma burada tamamlanir.
- Token saklama yeri ve XSS degerlendirmesi `architecture.md`/`CLAUDE.md` §5-§6 kurallarina uymalidir; karar devlog'da gerekcelendirilmelidir.
- `PublicReportPage` ve `ReportDetailPage` bilesenleri zaten vardir; bu ticket onlari rota/guard duzenine baglar, yeniden yazmaz.
