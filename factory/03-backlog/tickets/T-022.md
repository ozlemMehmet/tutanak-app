# T-022: Abonelik durumu ve odeme baslatma ekrani

- **Repo:** main
- **QA modu:** komsulu
- **Boyut:** S
- **Bagimli oldugu ticketlar:** T-017
- **Iliskili PRD maddeleri:** H-09, Kapsam ici madde 9
- **Durum:** todo

## Kabul Kriterleri (test edilebilir)
- [ ] `/subscription` acildiginda `GET /me` cagrilir ve abonelik durumu bir durum karti + rozet olarak gosterilir (`inactive` notr, `pending` uyari, `active` basari tonunda).
- [ ] `status === 'inactive'` iken "Odeme Yap" butonu aktiftir; tiklaninca `POST /billing/checkout` cagrilir ve donen `checkoutUrl`'e YENI SEKME DEGIL tam sayfa yonlendirme yapilir.
- [ ] `status === 'pending'` iken "Odeme sonucu bekleniyor, abonelik henuz aktif degil" bilgi metni gosterilir ve odeme butonu gizli/disabled olur.
- [ ] `status === 'active'` iken "Aboneliginiz aktif" metni ve `currentPeriodEnd` okunur tarih bicimiyle gosterilir; odeme butonu gosterilmez.
- [ ] Saglayicidan donuste sayfa, `?checkout=return` sorgu parametresini veya sekme gorunurluk olayini yakalayip `GET /me`'yi otomatik yeniden ceker; kullanici manuel yenileme yapmak zorunda kalmaz.
- [ ] Checkout 502 `PAYMENT_PROVIDER_ERROR` dondugunde "Odeme saglayicisina ulasilamadi, tekrar deneyin" banner'i gosterilir.
- [ ] Checkout 409 `SUBSCRIPTION_ALREADY_ACTIVE` dondugunde bilgi banner'i gosterilir ve `GET /me` yeniden cekilir.

## Kapsam
`design.md` §3 SubscriptionPage sartnamesi. Backend (T-012) checkout + webhook + `/me` akisini
karsiliyor; eksik olan kullanici arayuzudur. Entegrasyon QA'sinde "Kullanici abonelik odemesi
yapabiliyor" DoD maddesi bu yuzden FAIL verdi.

PRD baglami: "H-09 | Selin | ... abonelik odemesini yapip hesabimin aktif oldugunu gormek istiyorum."

## Kapsam DISI
- Odeme saglayicisi entegrasyonu, webhook dogrulama ve abonelik durum makinesi — T-012.
- Fatura/makbuz goruntuleme, plan degistirme, abonelik iptali — PRD MVP kapsaminda degil.

## Teknik Notlar (varsa)
- `GET /me`'nin `subscription` alani her zaman doludur (varsayilan `inactive` nesnesi dahil), bu yuzden bos durum tasarimi gerekmez.
- Tutar ve para birimi sunucudan gelir; koda gomulmez.
