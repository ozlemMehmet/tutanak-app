// ReportListPage (`/reports`) — design.md §1 Ekran Envanteri. Liste, arama ve sayfalama
// T-021 kapsamindadir; burada yalnizca AppShell altindaki rota iskeleti vardir.
export function ReportListPage(): React.JSX.Element {
  return (
    <main className="page">
      <h1>Tutanaklarim</h1>
    </main>
  );
}
