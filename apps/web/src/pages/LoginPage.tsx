// LoginPage (`/login`) — design.md §1 Ekran Envanteri. Bu ticket (T-017) yalnizca rotayi ve
// oturum iskeletini teslim eder; form alanlari, hata durumlari, token'i saklama ve
// `redirectTo` hedefine donus T-018'de eklenir.
export function LoginPage(): React.JSX.Element {
  return (
    <main className="page">
      <h1>Giris Yap</h1>
    </main>
  );
}
